/* AI外卖助手对话页面样式 */

/* 温暖液态玻璃主容器 */
.chat-container {
  height: 100vh;
  background: linear-gradient(135deg, #FFFEF5 0%, #FFF9E6 100%);
  display: flex;
  flex-direction: column;
  position: relative;
  overflow: hidden;
}

/* 温暖氛围光 */
.chat-container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: 
    radial-gradient(circle at 20% 80%, rgba(255, 235, 59, 0.08) 0%, transparent 60%),
    radial-gradient(circle at 80% 20%, rgba(255, 248, 225, 0.06) 0%, transparent 60%),
    radial-gradient(circle at 40% 40%, rgba(255, 255, 255, 0.1) 0%, transparent 60%);
  animation: warmBreathing 10s ease-in-out infinite;
  pointer-events: none;
}

@keyframes warmBreathing {
  0%, 100% { opacity: 0.6; transform: translateY(0) scale(1); }
  50% { opacity: 0.9; transform: translateY(-8rpx) scale(1.02); }
}

/* 居中内容区域 */
.center-content {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 60rpx 24rpx;
  margin-top: 88rpx;
}

.smart-preset-container-center {
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 40rpx;
}

/* 金属质感标题 */
.metallic-title {
  margin-bottom: 24rpx;
}

.metallic-text {
  font-family: 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
  font-size: 32rpx;
  font-weight: 500;
  letter-spacing: 1rpx;
  color: #C8C8C8;
  position: relative;
  animation: metallicShine 3s ease-in-out infinite;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
  overflow: hidden;
}

.metallic-text::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
  animation: metallicGloss 2s ease-in-out infinite;
}

@keyframes metallicShine {
  0%, 100% {
    opacity: 0.9;
  }
  50% {
    opacity: 1;
  }
}

@keyframes metallicGloss {
  0% {
    left: -100%;
  }
  100% {
    left: 100%;
  }
}



/* 聊天消息区域 */
.chat-messages {
  flex: 1;
  padding: 40rpx 30rpx;
  overflow-y: auto;
}

.chat-messages.no-input {
  padding-bottom: 40rpx;
}

.chat-messages.with-input {
  padding-bottom: 200rpx;
}

.message-item {
  margin-bottom: 32rpx;
  animation: messageSlideIn 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

@keyframes messageSlideIn {
  from {
    transform: translateY(20rpx);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

/* 温暖通知卡片 - AI管家风格 */
.agent-message {
  display: flex;
  justify-content: flex-start;
  align-items: center;
  position: relative;
  margin: 20rpx 20rpx;
}

.agent-bubble {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(24rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  color: #2D3748;
  border-radius: 20rpx 20rpx 20rpx 0;
  padding: 32rpx 40rpx;
  width: auto;
  max-width: 600rpx;
  box-shadow: 
    0 16rpx 48rpx rgba(0, 0, 0, 0.08),
    0 8rpx 24rpx rgba(0, 0, 0, 0.04),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  position: relative;
  animation: notificationSlideIn 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
  text-align: left;
  opacity: 0.7;
}

@keyframes notificationSlideIn {
  0% {
    opacity: 0;
    transform: translateY(40rpx) scale(0.9);
  }
  70% {
    transform: translateY(-4rpx) scale(1.02);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.agent-bubble::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3rpx;
  background: linear-gradient(90deg, 
    transparent 0%,
    rgba(255, 154, 158, 0.6) 20%,
    rgba(250, 208, 196, 0.6) 80%,
    transparent 100%);
  border-radius: 20rpx 20rpx 0 0;
  animation: topShimmer 3s ease-in-out infinite;
}

@keyframes topShimmer {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}

/* 用户输入回显 - 极简风格 */
.user-message {
  display: flex;
  justify-content: flex-end;
  align-items: center;
  position: relative;
  margin: 20rpx 20rpx;
}

.user-bubble {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(16rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  color: #4A5568;
  border-radius: 20rpx 20rpx 0 20rpx;
  padding: 20rpx 32rpx;
  max-width: 400rpx;
  box-shadow: 
    0 12rpx 32rpx rgba(0, 0, 0, 0.06),
    0 6rpx 16rpx rgba(0, 0, 0, 0.03),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  position: relative;
  animation: gentleAppear 0.5s ease-out;
  text-align: right;
  font-size: 26rpx;
  opacity: 0.7;
}

/* 信息流区域 */
.information-flow {
  flex: 1;
  padding: 30rpx 20rpx;
  overflow-y: auto;
}

/* 通知项目样式 */
.notification-item {
  margin-bottom: 20rpx;
}

.information-flow.no-input {
  padding-bottom: 60rpx;
}

.information-flow.with-input {
  padding-bottom: 240rpx;
}

/* AI工作状态指示器 */
.working-indicator {
  display: flex;
  justify-content: center;
  margin: 40rpx 20rpx;
}

.working-status {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(16rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 20rpx;
  padding: 24rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
  box-shadow: 
    0 12rpx 32rpx rgba(0, 0, 0, 0.06),
    0 6rpx 16rpx rgba(0, 0, 0, 0.03),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  animation: workingPulse 2s ease-in-out infinite;
  opacity: 0.7;
}

@keyframes workingPulse {
  0%, 100% { opacity: 0.8; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.02); }
}

.working-text {
  color: #4A5568;
  font-size: 24rpx;
  opacity: 0.8;
  text-align: center;
}


.smart-input-field {
  width: 100%;
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 28rpx;
  padding: 24rpx 28rpx;
  font-size: 32rpx;
  font-weight: 600;
  color: #2D3748;
  line-height: 1.4;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  box-shadow: 
    0 12rpx 32rpx rgba(0, 0, 0, 0.06),
    0 6rpx 16rpx rgba(0, 0, 0, 0.03),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  outline: none;
  position: fixed;
  bottom: 60rpx;
  left: 24rpx;
  right: 24rpx;
  z-index: 1001;
  -webkit-user-select: text;
  user-select: text;
  -webkit-tap-highlight-color: transparent;
  box-sizing: border-box;
  min-height: 100rpx;
  overflow: visible;
  opacity: 0.7;
}

.smart-input-field::placeholder {
  color: #A0AEC0;
  opacity: 0.7;
  font-size: 28rpx;
  font-weight: 500;
  overflow: visible;
}

.smart-input-field:focus {
  border-color: rgba(255, 255, 255, 0.2);
  background: rgba(255, 255, 255, 0.8);
  box-shadow: 
    0 16rpx 40rpx rgba(0, 0, 0, 0.08),
    0 8rpx 20rpx rgba(0, 0, 0, 0.04),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.15);
  transform: scale(1.02);
  opacity: 0.8;
}

.smart-input-field::placeholder {
  color: #A0AEC0;
  font-weight: 400;
}

/* 输入框呼吸边框 */
.input-breathing-border {
  position: absolute;
  top: -2rpx;
  left: -2rpx;
  right: -2rpx;
  bottom: -2rpx;
  border-radius: 30rpx;
  background: linear-gradient(135deg, 
    rgba(255, 154, 158, 0.3) 0%,
    rgba(250, 208, 196, 0.3) 100%);
  animation: inputBreathing 3s ease-in-out infinite;
  pointer-events: none;
  z-index: -1;
}

@keyframes inputBreathing {
  0%, 100% { 
    opacity: 0.3;
    transform: scale(1);
  }
  50% { 
    opacity: 0.6;
    transform: scale(1.02);
  }
}

/* 快速提示 */
.quick-hints {
  margin-top: 16rpx;
  text-align: center;
}

.hint-text {
  color: #A0AEC0;
  font-size: 22rpx;
  opacity: 0.8;
  animation: hintFade 4s ease-in-out infinite;
}

@keyframes hintFade {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 0.9; }
}

/* 全局涟漪效果 */
.ripple-container {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
  z-index: 50;
  overflow: hidden;
}

@keyframes gentleAppear {
  0% {
    opacity: 0;
    transform: translateY(20rpx) scale(0.95);
  }
  100% {
    opacity: 0.9;
    transform: translateY(0) scale(1);
  }
}

/* 消息文本样式 */
.message-text {
  font-size: 28rpx;
  line-height: 1.5;
  word-wrap: break-word;
  font-weight: 400;
}

/* 选项按钮样式 */
.option-buttons {
  margin-top: 24rpx;
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.option-btn {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 50rpx;
  padding: 16rpx 32rpx;
  color: #2D3748;
  font-size: 28rpx;
  font-weight: 500;
  transition: all 0.3s ease;
  opacity: 0.7;
  box-shadow: 
    0 8rpx 24rpx rgba(0, 0, 0, 0.06),
    0 4rpx 12rpx rgba(0, 0, 0, 0.03),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.option-btn:hover {
  background: rgba(255, 255, 255, 0.08);
  border-color: rgba(255, 255, 255, 0.2);
  transform: translateY(-2rpx);
  opacity: 0.85;
}

.option-btn::after {
  border: none;
}

/* 口味偏好选项样式 */
.taste-options {
  margin-top: 24rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

/* 智能预设显示 - 取消传统按钮 */
.smart-preset-container {
  display: flex;
  justify-content: center;
  margin: 20rpx 0;
}

.smart-preset-tag {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(12rpx);
  border: 1rpx solid rgba(255, 154, 158, 0.2);
  border-radius: 20rpx;
  padding: 12rpx 24rpx;
  font-size: 24rpx;
  font-weight: 400;
  color: #4A5568;
  text-align: center;
  animation: smartTagBreathing 2s ease-in-out infinite;
  box-shadow: 
    0 4rpx 16rpx rgba(255, 154, 158, 0.1),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.4);
  position: relative;
  opacity: 0.8;
}

@keyframes smartTagBreathing {
  0%, 100% { opacity: 0.8; transform: scale(1); }
  50% { opacity: 0.95; transform: scale(1.02); }
}

/* 更大更显眼的预设标签 */
.smart-preset-tag-large {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 28rpx;
  padding: 32rpx 48rpx;
  font-size: 36rpx;
  font-weight: 600;
  color: #2D3748;
  text-align: center;
  animation: smartTagBreathing 2s ease-in-out infinite;
  box-shadow: 
    0 16rpx 48rpx rgba(0, 0, 0, 0.12),
    0 8rpx 24rpx rgba(0, 0, 0, 0.06),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  position: relative;
  opacity: 0.95;
  line-height: 1.6;
  word-wrap: break-word;
  white-space: normal;
  overflow: visible;
  pointer-events: none;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.preset-label {
  color: #A0AEC0;
  font-weight: 400;
  display: inline;
}

.preset-line {
  display: block;
  margin-top: 8rpx;
}

/* 动态毛玻璃透明度渐变 */
.dynamic-glass {
  animation: glassOpacityFlow 4s ease-in-out infinite;
}

@keyframes glassOpacityFlow {
  0%, 100% { 
    background: rgba(255, 255, 255, 0.02);
    opacity: 0.7;
  }
  50% { 
    background: rgba(255, 255, 255, 0.08);
    opacity: 0.95;
  }
}

/* 中央文案 - 嵌入背景底层 */
.center-text {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  z-index: -1;
  pointer-events: none;
  opacity: 0.15;
}

.center-line {
  display: block;
  font-family: 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
  color: #666666;
  font-weight: 700;
  font-size: 40rpx;
  letter-spacing: 2rpx;
  margin-bottom: 16rpx;
  text-align: center;
  line-height: 1.2;
}

/* 底部提示文字 */
.bottom-hint {
  position: fixed;
  bottom: 180rpx;
  left: 24rpx;
  right: 24rpx;
  text-align: center;
  z-index: 999;
}

.hint-text {
  color: #A0AEC0;
  font-size: 24rpx;
  font-weight: 400;
  opacity: 0.8;
  line-height: 1.4;
}

.time-option-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
  transition: left 0.6s ease;
}

.time-option-btn:hover {
  background: rgba(59, 130, 246, 0.2);
  border-color: rgba(59, 130, 246, 0.4);
  color: #FFFFFF;
  transform: translateY(-3rpx) scale(1.02);
  box-shadow: 
    0 12rpx 32rpx rgba(59, 130, 246, 0.25),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
}

.time-option-btn:hover::before {
  left: 100%;
}

.time-option-btn::after {
  border: none;
}

/* 隐藏选项 - 取消传统选择界面 */
.hidden-options {
  display: none;
}

.taste-option::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, 
    rgba(168, 85, 247, 0.03) 0%, 
    rgba(59, 130, 246, 0.02) 100%);
  transition: opacity 0.3s ease;
  opacity: 0;
}

.taste-option:hover {
  background: rgba(120, 119, 198, 0.15);
  border-color: rgba(120, 119, 198, 0.3);
  transform: translateY(-4rpx) scale(1.01);
  box-shadow: 
    0 16rpx 40rpx rgba(120, 119, 198, 0.2),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.15);
}

.taste-option:hover::before {
  opacity: 1;
}

.taste-option::after {
  border: none;
}

.option-title {
  color: #E8E9F0;
  font-size: 30rpx;
  font-weight: 600;
  margin-bottom: 8rpx;
  position: relative;
  z-index: 1;
}

.option-subtitle {
  color: rgba(232, 233, 240, 0.7);
  font-size: 24rpx;
  font-weight: 400;
  position: relative;
  z-index: 1;
}

/* 最终确认消息样式 */
.final-bubble {
  background: linear-gradient(135deg, #34D399 0%, #10B981 100%);
}

.final-text {
  font-size: 30rpx;
  font-weight: 600;
  color: #FFFFFF;
  display: block;
  margin-bottom: 16rpx;
}

.order-link {
  color: rgba(255, 255, 255, 0.8);
  font-size: 28rpx;
  font-weight: 500;
  text-decoration: underline;
}

/* 打字指示器样式 */
.typing-indicator {
  display: flex;
  justify-content: flex-start;
  margin-bottom: 32rpx;
}

.typing-dots {
  background: rgba(91, 105, 232, 0.1);
  border-radius: 24rpx;
  padding: 20rpx 32rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.dot {
  width: 8rpx;
  height: 8rpx;
  border-radius: 50%;
  background: #5B69E8;
  animation: typingDots 1.4s infinite ease-in-out;
}

.dot1 { animation-delay: 0s; }
.dot2 { animation-delay: 0.2s; }
.dot3 { animation-delay: 0.4s; }

@keyframes typingDots {
  0%, 80%, 100% {
    transform: scale(0.8);
    opacity: 0.5;
  }
  40% {
    transform: scale(1.2);
    opacity: 1;
  }
}

/* 底部输入区域样式 - 玻璃拟物风格 */
.input-area {
  background: rgba(29, 26, 59, 0.8);
  backdrop-filter: blur(20rpx);
  border-top: 1rpx solid rgba(255, 255, 255, 0.1);
  padding: 24rpx 30rpx;
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
  box-shadow: 
    0 -8rpx 32rpx rgba(0, 0, 0, 0.2),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.05);
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 100;
}

.input-area::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, 
    rgba(168, 85, 247, 0.05) 0%, 
    rgba(59, 130, 246, 0.03) 100%);
  pointer-events: none;
}

/* 预算能量条样式 */
.budget-energy-container {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
  position: relative;
  z-index: 1;
}

.energy-bar-wrapper {
  flex: 1;
}

.energy-labels {
  display: flex;
  justify-content: space-between;
  margin-bottom: 16rpx;
}

.energy-label-left,
.energy-label-right {
  font-size: 24rpx;
  color: rgba(232, 233, 240, 0.7);
  font-weight: 500;
}

.energy-bar-track {
  position: relative;
  height: 8rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 8rpx;
  margin: 32rpx 0;
  overflow: visible;
  backdrop-filter: blur(4rpx);
}

.energy-bar-fill {
  height: 100%;
  background: linear-gradient(90deg, 
    rgba(34, 197, 94, 0.8) 0%,
    rgba(59, 130, 246, 0.8) 50%,
    rgba(168, 85, 247, 0.8) 100%);
  border-radius: 8rpx;
  transition: width 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  box-shadow: 0 0 16rpx rgba(59, 130, 246, 0.3);
}

.energy-bar-thumb {
  position: absolute;
  top: -16rpx;
  transform: translateX(-50%);
  width: 40rpx;
  height: 40rpx;
  background: rgba(255, 255, 255, 0.9);
  border: 3rpx solid rgba(59, 130, 246, 0.8);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  box-shadow: 
    0 8rpx 24rpx rgba(59, 130, 246, 0.3),
    0 0 0 0 rgba(59, 130, 246, 0.4);
  animation: thumbPulse 2s ease-in-out infinite;
}

@keyframes thumbPulse {
  0%, 100% {
    box-shadow: 
      0 8rpx 24rpx rgba(59, 130, 246, 0.3),
      0 0 0 0 rgba(59, 130, 246, 0.4);
  }
  50% {
    box-shadow: 
      0 8rpx 24rpx rgba(59, 130, 246, 0.3),
      0 0 0 8rpx rgba(59, 130, 246, 0.2);
  }
}

.energy-thumb-glow {
  position: absolute;
  top: -4rpx;
  left: -4rpx;
  right: -4rpx;
  bottom: -4rpx;
  background: radial-gradient(circle, rgba(59, 130, 246, 0.3) 0%, transparent 70%);
  border-radius: 50%;
  animation: glowPulse 2s ease-in-out infinite;
}

@keyframes glowPulse {
  0%, 100% { opacity: 0.6; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.2); }
}

.energy-value {
  position: absolute;
  top: -50rpx;
  left: 50%;
  transform: translateX(-50%);
  background: rgba(29, 26, 59, 0.9);
  backdrop-filter: blur(12rpx);
  color: #E8E9F0;
  padding: 8rpx 16rpx;
  border-radius: 16rpx;
  font-size: 24rpx;
  font-weight: 600;
  border: 1rpx solid rgba(59, 130, 246, 0.3);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.2);
  white-space: nowrap;
}

.energy-value::after {
  content: '';
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 8rpx solid transparent;
  border-right: 8rpx solid transparent;
  border-top: 8rpx solid rgba(29, 26, 59, 0.9);
}

.energy-range-hints {
  display: flex;
  justify-content: space-between;
  margin-top: 16rpx;
}

.range-hint {
  font-size: 20rpx;
  color: rgba(232, 233, 240, 0.5);
  font-weight: 400;
}

/* 风味矩阵样式 */
.flavor-matrix-container {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
  position: relative;
  z-index: 1;
}

.matrix-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #E8E9F0;
  text-align: center;
  margin-bottom: 8rpx;
}

.matrix-subtitle {
  font-size: 24rpx;
  color: rgba(232, 233, 240, 0.7);
  text-align: center;
  margin-bottom: 24rpx;
}

.matrix-canvas {
  position: relative;
  width: 100%;
  height: 200rpx;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(16rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 20rpx;
  overflow: hidden;
}

.matrix-bg {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, 
    rgba(34, 197, 94, 0.1) 0%,
    rgba(59, 130, 246, 0.1) 50%,
    rgba(168, 85, 247, 0.1) 100%);
  animation: matrixFlow 8s ease-in-out infinite;
}

@keyframes matrixFlow {
  0%, 100% { 
    background: linear-gradient(135deg, 
      rgba(34, 197, 94, 0.1) 0%,
      rgba(59, 130, 246, 0.1) 50%,
      rgba(168, 85, 247, 0.1) 100%);
  }
  50% { 
    background: linear-gradient(135deg, 
      rgba(168, 85, 247, 0.1) 0%,
      rgba(59, 130, 246, 0.1) 50%,
      rgba(34, 197, 94, 0.1) 100%);
  }
}

.matrix-axes {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
}

.axis-labels {
  position: relative;
  width: 100%;
  height: 100%;
}

.axis-label {
  position: absolute;
  font-size: 20rpx;
  color: rgba(232, 233, 240, 0.6);
  font-weight: 500;
}

.axis-label.top {
  top: 16rpx;
  left: 50%;
  transform: translateX(-50%);
}

.axis-label.bottom {
  bottom: 16rpx;
  left: 50%;
  transform: translateX(-50%);
}

.axis-label.left {
  left: 16rpx;
  top: 50%;
  transform: translateY(-50%);
}

.axis-label.right {
  right: 16rpx;
  top: 50%;
  transform: translateY(-50%);
}

.flavor-pointer {
  position: absolute;
  width: 32rpx;
  height: 32rpx;
  transform: translate(-50%, -50%);
  cursor: pointer;
  z-index: 10;
}

.pointer-glow {
  position: absolute;
  top: -8rpx;
  left: -8rpx;
  right: -8rpx;
  bottom: -8rpx;
  background: radial-gradient(circle, rgba(168, 85, 247, 0.4) 0%, transparent 70%);
  border-radius: 50%;
  animation: pointerPulse 2s ease-in-out infinite;
}

@keyframes pointerPulse {
  0%, 100% { opacity: 0.6; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.3); }
}

.pointer-center {
  position: absolute;
  top: 8rpx;
  left: 8rpx;
  width: 16rpx;
  height: 16rpx;
  background: #FFFFFF;
  border: 2rpx solid rgba(168, 85, 247, 0.8);
  border-radius: 50%;
  box-shadow: 
    0 4rpx 16rpx rgba(168, 85, 247, 0.4),
    inset 0 2rpx 4rpx rgba(255, 255, 255, 0.3);
}

.matrix-visualization {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: auto;
}

.flavor-result {
  background: rgba(120, 119, 198, 0.1);
  backdrop-filter: blur(12rpx);
  border: 1rpx solid rgba(120, 119, 198, 0.2);
  border-radius: 16rpx;
  padding: 20rpx;
  text-align: center;
}

.result-text {
  color: #E8E9F0;
  font-size: 28rpx;
  font-weight: 500;
  line-height: 1.4;
}

.budget-field {
  flex: 1;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(12rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 24rpx;
  padding: 24rpx 32rpx;
  font-size: 32rpx;
  color: #E8E9F0;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  z-index: 10;
  min-height: 80rpx;
  box-shadow: 
    0 4rpx 16rpx rgba(0, 0, 0, 0.1),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.05);
}

.budget-field:focus {
  border-color: rgba(59, 130, 246, 0.5);
  background: rgba(255, 255, 255, 0.08);
  box-shadow: 
    0 0 0 6rpx rgba(59, 130, 246, 0.15),
    0 8rpx 24rpx rgba(59, 130, 246, 0.2),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.budget-field::placeholder {
  color: rgba(232, 233, 240, 0.5);
}

.currency-symbol {
  font-size: 36rpx;
  font-weight: 700;
  color: #7C87F0;
  text-shadow: 0 2rpx 8rpx rgba(124, 135, 240, 0.3);
}

/* 链接输入样式 */
.link-input {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.link-field {
  flex: 1;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(12rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 24rpx;
  padding: 24rpx 32rpx;
  font-size: 28rpx;
  color: #E8E9F0;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  z-index: 10;
  min-height: 80rpx;
  box-shadow: 
    0 4rpx 16rpx rgba(0, 0, 0, 0.1),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.05);
}

.link-field:focus {
  border-color: rgba(168, 85, 247, 0.5);
  background: rgba(255, 255, 255, 0.08);
  box-shadow: 
    0 0 0 6rpx rgba(168, 85, 247, 0.15),
    0 8rpx 24rpx rgba(168, 85, 247, 0.2),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.link-field::placeholder {
  color: rgba(232, 233, 240, 0.5);
}

/* 文本输入样式 */
.text-input {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.text-field {
  flex: 1;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(12rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 24rpx;
  padding: 24rpx 32rpx;
  font-size: 30rpx;
  color: #E8E9F0;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  z-index: 10;
  min-height: 80rpx;
  box-shadow: 
    0 4rpx 16rpx rgba(0, 0, 0, 0.1),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.05);
}

.text-field:focus {
  border-color: rgba(120, 119, 198, 0.5);
  background: rgba(255, 255, 255, 0.08);
  box-shadow: 
    0 0 0 6rpx rgba(120, 119, 198, 0.15),
    0 8rpx 24rpx rgba(120, 119, 198, 0.2),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.text-field::placeholder {
  color: rgba(232, 233, 240, 0.5);
}

/* 发送按钮样式 - 流体数据主义 */
.send-btn {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 50%;
  width: 80rpx;
  height: 80rpx;
  font-size: 28rpx;
  font-weight: 600;
  color: #2D3748;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  box-shadow: 
    0 8rpx 24rpx rgba(0, 0, 0, 0.06),
    0 4rpx 12rpx rgba(0, 0, 0, 0.03),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  position: relative;
  overflow: hidden;
  opacity: 0.7;
}

.send-btn::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  background: radial-gradient(circle, rgba(168, 85, 247, 0.3) 0%, transparent 70%);
  border-radius: 50%;
  transition: all 0.3s ease;
  transform: translate(-50%, -50%);
}

.send-btn.active {
  background: linear-gradient(135deg, rgba(168, 85, 247, 0.8) 0%, rgba(59, 130, 246, 0.8) 100%);
  border-color: rgba(168, 85, 247, 0.4);
  color: #FFFFFF;
  transform: scale(1.08);
  box-shadow: 
    0 8rpx 32rpx rgba(168, 85, 247, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
}

.send-btn.active::before {
  width: 60rpx;
  height: 60rpx;
}

.send-btn:disabled {
  opacity: 0.4;
  transform: scale(1);
}

.send-btn::after {
  border: none;
}

/* 结果通知卡片样式已移除 - 现在使用 agent-bubble 样式 */

/* 原 result-notification 相关样式已移除 */

.countdown-container {
  margin-top: 24rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid rgba(255, 255, 255, 0.15);
  display: flex;
  flex-direction: column;
  align-items: center;
}

.countdown-circle {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 16rpx;
  position: relative;
  animation: countdownPulse 1s ease-in-out infinite;
  box-shadow: 
    0 8rpx 24rpx rgba(0, 0, 0, 0.06),
    0 4rpx 12rpx rgba(0, 0, 0, 0.03),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  opacity: 0.7;
}

@keyframes countdownPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

.countdown-number {
  color: #2D3748;
  font-size: 32rpx;
  font-weight: 700;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
}

.countdown-text {
  color: #4A5568;
  font-size: 22rpx;
  opacity: 0.8;
}

/* 数字餐盘样式 - P1级别优化 */
.digital-plate-container {
  width: 100%;
  margin: 32rpx 20rpx;
  display: flex;
  justify-content: center;
}

.plate-card {
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(24rpx);
  border: 1rpx solid rgba(255, 154, 158, 0.2);
  border-radius: 24rpx;
  overflow: hidden;
  width: 100%;
  max-width: 500rpx;
  box-shadow: 
    0 16rpx 64rpx 0 rgba(255, 154, 158, 0.12),
    inset 0 0 0 1rpx rgba(255, 255, 255, 0.4);
  animation: plateSlideIn 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
  position: relative;
}

@keyframes plateSlideIn {
  0% {
    opacity: 0;
    transform: translateY(40rpx) scale(0.95);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.plate-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3rpx;
  background: linear-gradient(90deg, 
    transparent 0%,
    #FF9A9E 20%,
    #FAD0C4 80%,
    transparent 100%);
  animation: topGlow 3s ease-in-out infinite;
}

@keyframes topGlow {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}

.dish-image-container {
  position: relative;
  width: 100%;
  height: 300rpx;
  overflow: hidden;
  background: linear-gradient(135deg, 
    rgba(34, 197, 94, 0.1) 0%,
    rgba(59, 130, 246, 0.1) 50%,
    rgba(168, 85, 247, 0.1) 100%);
}

.dish-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s ease;
}

.dish-image:hover {
  transform: scale(1.05);
}

.image-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(180deg, 
    rgba(0, 0, 0, 0.1) 0%,
    rgba(0, 0, 0, 0.3) 100%);
  display: flex;
  align-items: flex-start;
  justify-content: flex-end;
  padding: 20rpx;
}

.rating-badge {
  background: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(12rpx);
  border-radius: 20rpx;
  padding: 8rpx 16rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.rating-star {
  font-size: 24rpx;
}

.rating-score {
  color: #FFFFFF;
  font-size: 24rpx;
  font-weight: 600;
}

.dish-info {
  padding: 32rpx;
  background: rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(16rpx);
}

.dish-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 16rpx;
}

.dish-name {
  color: #2D3748;
  font-size: 36rpx;
  font-weight: 700;
  flex: 1;
  margin-right: 16rpx;
  line-height: 1.3;
}

.dish-price {
  color: #FF6B6B;
  font-size: 32rpx;
  font-weight: 800;
  text-shadow: 0 2rpx 8rpx rgba(255, 107, 107, 0.3);
}

.dish-description {
  color: #4A5568;
  font-size: 28rpx;
  line-height: 1.5;
  margin-bottom: 24rpx;
  opacity: 0.8;
}

.dish-metrics {
  margin-bottom: 24rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.metric-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.metric-label {
  font-size: 28rpx;
  width: 48rpx;
  text-align: center;
}

.metric-bar {
  flex: 1;
  height: 8rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 8rpx;
  overflow: hidden;
  position: relative;
}

.metric-fill {
  height: 100%;
  border-radius: 8rpx;
  transition: width 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
}

.metric-fill::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(90deg, 
    transparent 0%,
    rgba(255, 255, 255, 0.2) 50%,
    transparent 100%);
  animation: shimmer 2s ease-in-out infinite;
}

@keyframes shimmer {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

.metric-fill.spicy {
  background: linear-gradient(90deg, 
    rgba(239, 68, 68, 0.8) 0%,
    rgba(245, 101, 101, 0.8) 100%);
  box-shadow: 0 0 12rpx rgba(239, 68, 68, 0.4);
}

.metric-fill.sweet {
  background: linear-gradient(90deg, 
    rgba(251, 191, 36, 0.8) 0%,
    rgba(252, 211, 77, 0.8) 100%);
  box-shadow: 0 0 12rpx rgba(251, 191, 36, 0.4);
}

.metric-value {
  color: #4A5568;
  font-size: 24rpx;
  font-weight: 600;
  min-width: 60rpx;
  text-align: right;
  opacity: 0.8;
}

.dish-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 24rpx;
}

.dish-tag {
  background: rgba(255, 154, 158, 0.15);
  backdrop-filter: blur(8rpx);
  border: 1rpx solid rgba(255, 154, 158, 0.3);
  color: #4A5568;
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  font-weight: 500;
  transition: all 0.3s ease;
  opacity: 0.9;
}

.dish-tag:hover {
  background: rgba(120, 119, 198, 0.25);
  border-color: rgba(120, 119, 198, 0.5);
}

.restaurant-info {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32rpx;
  padding: 16rpx 0;
  border-top: 1rpx solid rgba(255, 255, 255, 0.1);
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.1);
}

.restaurant-name {
  color: #4A5568;
  font-size: 26rpx;
  font-weight: 500;
  opacity: 0.8;
}

.delivery-time {
  color: #FF6B6B;
  font-size: 26rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.delivery-time::before {
  content: '🚀';
  font-size: 20rpx;
}

.order-button {
  width: 100%;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 50rpx;
  padding: 24rpx;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  box-shadow: 
    0 8rpx 24rpx rgba(0, 0, 0, 0.06),
    0 4rpx 12rpx rgba(0, 0, 0, 0.03),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  position: relative;
  overflow: hidden;
  opacity: 0.7;
  color: #2D3748;
}

.order-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, 
    transparent 0%,
    rgba(255, 255, 255, 0.2) 50%,
    transparent 100%);
  transition: left 0.6s ease;
}

.order-button:hover {
  background: linear-gradient(135deg, #FF8A8A 0%, #F5C6B8 100%);
  transform: translateY(-4rpx) scale(1.02);
  box-shadow: 
    0 16rpx 48rpx rgba(255, 154, 158, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.4);
}

.order-button:hover::before {
  left: 100%;
}

.order-button::after {
  border: none;
}

.button-content {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  position: relative;
  z-index: 1;
}

.button-text {
  color: #FFFFFF;
  font-size: 32rpx;
  font-weight: 700;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
}

.button-icon {
  font-size: 28rpx;
  animation: rocketBounce 2s ease-in-out infinite;
}

@keyframes rocketBounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-4rpx); }
}

/* 高级呼吸感动效系统 - 透明管家革命性体验 */
.smart-input-field.breathing {
  animation: fieldBreathing 4s ease-in-out infinite;
}

@keyframes fieldBreathing {
  0%, 100% {
    box-shadow: 
      0 8rpx 32rpx rgba(255, 154, 158, 0.1),
      inset 0 0 0 1rpx rgba(255, 255, 255, 0.4);
  }
  50% {
    box-shadow: 
      0 12rpx 48rpx rgba(255, 154, 158, 0.15),
      inset 0 0 0 1rpx rgba(255, 255, 255, 0.5);
  }
}

/* 智能预设标签呼吸 */
.smart-preset-tag.breathing {
  animation: tagBreathing 3s ease-in-out infinite;
}

@keyframes tagBreathing {
  0%, 100% {
    opacity: 0.8;
    transform: scale(1);
    background: rgba(255, 255, 255, 0.7);
  }
  50% {
    opacity: 0.95;
    transform: scale(1.02);
    background: rgba(255, 255, 255, 0.8);
  }
}

/* 结果通知卡片呼吸 */
.result-notification.breathing {
  animation: resultBreathing 5s ease-in-out infinite;
}

@keyframes resultBreathing {
  0%, 100% {
    box-shadow: 
      0 16rpx 64rpx 0 rgba(255, 154, 158, 0.15),
      inset 0 0 0 1rpx rgba(255, 255, 255, 0.5);
  }
  50% {
    box-shadow: 
      0 20rpx 80rpx 0 rgba(255, 154, 158, 0.2),
      inset 0 0 0 1rpx rgba(255, 255, 255, 0.6);
  }
}

/* 增强触摸反馈系统 */
.enhanced-touch-feedback {
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  overflow: hidden;
}

.enhanced-touch-feedback::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  background: radial-gradient(circle, rgba(255, 154, 158, 0.3) 0%, transparent 70%);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  transition: all 0.3s ease;
  pointer-events: none;
}

.enhanced-touch-feedback:active {
  transform: scale(0.98);
}

.enhanced-touch-feedback:active::before {
  width: 200rpx;
  height: 200rpx;
}

/* 涟漪扩散效果 */
.ripple-expand {
  position: relative;
  overflow: hidden;
}

.ripple-expand::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 20rpx;
  height: 20rpx;
  background: rgba(255, 154, 158, 0.2);
  border-radius: 50%;
  transform: translate(-50%, -50%) scale(0);
  animation: rippleExpand 2s ease-out infinite;
}

@keyframes rippleExpand {
  0% {
    transform: translate(-50%, -50%) scale(0);
    opacity: 1;
  }
  100% {
    transform: translate(-50%, -50%) scale(20);
    opacity: 0;
  }
}

/* 背景涟漪动画 - 传达AI后台工作状态 */
.chat-container.processing::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 200rpx;
  height: 200rpx;
  background: radial-gradient(circle, rgba(255, 154, 158, 0.05) 0%, transparent 70%);
  border-radius: 50%;
  transform: translate(-50%, -50%) scale(0);
  animation: backgroundRipple 3s ease-out infinite;
  pointer-events: none;
  z-index: 1;
}

@keyframes backgroundRipple {
  0% {
    transform: translate(-50%, -50%) scale(0);
    opacity: 1;
  }
  100% {
    transform: translate(-50%, -50%) scale(10);
    opacity: 0;
  }
}

/* 微妙的悬浮效果 */
.gentle-hover {
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.gentle-hover:hover {
  transform: translateY(-2rpx) scale(1.005);
  box-shadow: 
    0 20rpx 80rpx rgba(255, 154, 158, 0.2),
    inset 0 0 0 1rpx rgba(255, 255, 255, 0.6);
}

/* 光晕脉动效果 */
.glow-pulse {
  position: relative;
}

.glow-pulse::before {
  content: '';
  position: absolute;
  top: -4rpx;
  left: -4rpx;
  right: -4rpx;
  bottom: -4rpx;
  background: linear-gradient(135deg, 
    rgba(255, 154, 158, 0.2) 0%,
    rgba(250, 208, 196, 0.2) 100%);
  border-radius: inherit;
  animation: glowPulse 3s ease-in-out infinite;
  pointer-events: none;
  z-index: -1;
}

@keyframes glowPulse {
  0%, 100% { opacity: 0.3; transform: scale(1); }
  50% { opacity: 0.6; transform: scale(1.05); }
}

/* 优雅的加载状态 */
.elegant-loading {
  position: relative;
}

.elegant-loading::after {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, 
    transparent 0%,
    rgba(255, 255, 255, 0.1) 50%,
    transparent 100%);
  animation: elegantShimmer 2s ease-in-out infinite;
}

@keyframes elegantShimmer {
  0% { left: -100%; }
  100% { left: 100%; }
}

/* 响应式适配 - 优化移动端体验 */
@media (max-width: 375px) {
  .header {
    padding: 40rpx 30rpx 20rpx;
  }

  .header-title {
    font-size: 28rpx;
  }

  .header-subtitle {
    font-size: 22rpx;
  }

  .information-flow {
    padding: 24rpx 16rpx;
  }

  .message-text {
    font-size: 26rpx;
  }

  .smart-input-area {
    padding: 24rpx 20rpx;
  }

  .smart-input-field {
    padding: 24rpx 28rpx;
    font-size: 30rpx;
  }

  /* 移动端呼吸动画优化 */
  .enhanced-touch-feedback:active::before {
    width: 150rpx;
    height: 150rpx;
  }
}

/* 减少动画的可访问性设置 */
@media (prefers-reduced-motion: reduce) {
  .breathing,
  .enhanced-touch-feedback,
  .ripple-expand,
  .glow-pulse,
  .elegant-loading {
    animation: none;
  }

  .smooth-transition,
  .enhanced-touch-feedback {
    transition: none;
  }
}

/* 高对比度模式支持 */
@media (prefers-contrast: high) {
  .smart-input-field {
    border-width: 3rpx;
    border-color: rgba(255, 154, 158, 0.8);
  }

  .result-notification {
    border-width: 2rpx;
    border-color: rgba(255, 154, 158, 0.6);
  }
}
